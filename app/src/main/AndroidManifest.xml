<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.motor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.motor.aboutActivity" android:parentActivityName="com.example.motor.MainActivity"></activity>
        <activity android:name="com.example.motor.detail_buahActivity" android:parentActivityName="com.example.motor.MainActivity"/>
        <activity android:name="com.example.motor.item_buahActivity" />
        <activity android:name="com.example.motor.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>